<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MANDUU.ViewModels"
             xmlns:icons="clr-namespace:MANDUU.Helpers"
             xmlns:models="clr-namespace:MANDUU.Models"
             x:Class="MANDUU.Views.MainPages.ShopCategoryPage"
             x:Name="ShopCategoryPageRoot"
             Title="Shop Categories">

    <Shell.TitleView>
        <Grid ColumnDefinitions="auto, *, auto"
          Padding="0, 0, 5, 0"
          Margin="0"
          VerticalOptions="Center">
            <Label Grid.Column="0" 
                   Text="{x:Static icons:Icons.profileIcon}"
                   Style="{StaticResource MaterialIcon}"
                   HorizontalOptions="Start"
                   FontSize="30">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding GoToProfileCommand}"/>
                </Label.GestureRecognizers>
            </Label>

            <Label Text="Shop Categories"
                   Grid.Column="1"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   Style="{StaticResource SubHeadline}"/>

            <HorizontalStackLayout Grid.Column="2" VerticalOptions="Center"
                               HorizontalOptions="End"
                               Spacing="10">
                <Grid VerticalOptions="Center" HorizontalOptions="End" WidthRequest="40" HeightRequest="40">
                    <!-- Cart Icon -->
                    <Label Text="{x:Static icons:Icons.cartIcon}"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"
                       Style="{StaticResource MaterialIcon}"
                       FontSize="30">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToCartCommand}"/>
                        </Label.GestureRecognizers>
                    </Label>

                    <!-- Badge Dot - Positioned Top Right -->
                    <Border BackgroundColor="Red"
                        HeightRequest="10"
                        WidthRequest="10"
                        StrokeThickness="0"
                        StrokeShape="RoundRectangle 5"
                        IsVisible="{Binding HasCartItems}"
                        HorizontalOptions="End"
                        VerticalOptions="Start"
                        Margin="0,0,2,2" />
                </Grid>
                <Label  
                   Text="{x:Static icons:Icons.searchIcon}"
                   HorizontalOptions="End"
                   FontFamily="MaterialIcon"
                   FontSize="30"
                VerticalTextAlignment="Center">
                </Label>
            </HorizontalStackLayout>
        </Grid>
    </Shell.TitleView>

    <Grid RowDefinitions="auto, *" Margin="5">
        <!-- My Shop button-->
        <Border Grid.Row="0"
                HorizontalOptions="End" 
                StrokeShape="RoundRectangle 5"
                BackgroundColor="{StaticResource Primary}"
                Padding="10"
                Margin="5,0"
                StrokeThickness="0">
            <Border.Shadow>
                <Shadow Brush="Gray"
                        Offset="5,5"
                        Opacity="0.5"/>
            </Border.Shadow>
            <Border.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding GoToMyShopCommand}"/>
            </Border.GestureRecognizers>
            <HorizontalStackLayout Spacing="5">
                <Label Text="{x:Static icons:Icons.shopIcon}"
                       FontFamily="MaterialIcon"
                       TextColor="#fff"
                       FontSize="20"/>
                <Label Text="My Shop" 
                       FontAttributes="Bold"
                       VerticalOptions="Center"
                       TextColor="#fff"/>
            </HorizontalStackLayout>
        </Border>

        <ScrollView Grid.Row="1">
            <CollectionView ItemsSource="{Binding ShopCategories}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:ShopCategory">
                        <Grid RowDefinitions="Auto, *" Margin="10,0,10,20">
                            <Border StrokeThickness="0"
                                    StrokeShape="RoundRectangle 5"
                                    Style="{x:StaticResource catTitleBorder}">
                                <Label Text="{Binding Name}"
                                       Style="{x:StaticResource CatTitleText}"/>
                            </Border>

                            <CollectionView Grid.Row="1"
                                            Margin="0,10"
                                            ItemsSource="{Binding Shops}">
                                <CollectionView.ItemsLayout>
                                    <LinearItemsLayout Orientation="Horizontal" ItemSpacing="10"/>
                                </CollectionView.ItemsLayout>
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="models:Shop">
                                        <Border Style="{StaticResource DisplayShopBorder}">
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer  Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.GoToSelectedShopProfileCommand}"
                                                                        CommandParameter="{Binding}"/>
                                            </Border.GestureRecognizers>
                                            <Grid RowDefinitions="*,auto">
                                                <Label Style="{StaticResource FavoriteIcon}"/>
                                                <BoxView BackgroundColor="#000" Opacity="0.2" ZIndex="1"/>
                                                <Image Source="{Binding ShopProfileImage}" Aspect="AspectFill"/>
                                                <VerticalStackLayout Grid.Row="1" Padding="5">
                                                    <Label Text="{Binding Name}" LineBreakMode="TailTruncation" MaxLines="1" Style="{StaticResource Title}"/>
                                                    <HorizontalStackLayout>
                                                        <Label Text="{x:Static icons:Icons.locationIcon}" Style="{StaticResource MaterialIcon}"/>
                                                        <Label Text="{Binding ShortLocation}"
                                                               FontFamily="Finlandica"/>
                                                    </HorizontalStackLayout>
                                                </VerticalStackLayout>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </ScrollView>
    </Grid>
</ContentPage>