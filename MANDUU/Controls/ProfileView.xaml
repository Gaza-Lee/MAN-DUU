<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MANDUU.Controls.ProfileView">
    <Border Padding="20"
            BackgroundColor="White"
            Stroke="Gray"
            StrokeThickness="1"
            Shadow="Medium">
        <VerticalStackLayout Spacing="20">
            <Label Text="👤 Gaza Lee" FontSize="22" />
            <Button Text="Edit Profile" Command="{Binding EditProfileCommand}" />
            <Button Text="Settings" Command="{Binding SettingsCommand}" />
            <Button Text="Logout" Command="{Binding LogoutCommand}" />
            <Button Text="Close" Clicked="OnCloseClicked" />
        </VerticalStackLayout>
    </Border>
</ContentView>
